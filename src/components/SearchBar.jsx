import React, { Component } from 'react';

class SearchBar extends Component {
  render() {
    return (
      <form className="form">
        <fieldset>
          <input type="text" />
        </fieldset>
      </form>
    );
  }
}

export default SearchBar;
