import React from 'react';
import GenInput from './GenInput';

class AddMovie extends React.Component {
  constructor(props) {
    super(props);
    this.handleChange = this.handleChange.bind(this);
    // this.genInput = this.genInput.bind(this);
    this.state = {
      subtitle: '',
      title: '',
      imagePath: '',
      storyLine: '',
      rating: 0,
      genre: 'action',
    };
  }

  handleChange({ target }) {
    const { name, value } = target;
    // this.setState({ [name]: value });
  }

  // genInput(type, id, dataTestId, value) {
  //   return (
  //     <input
  //       type={ type }
  //       id={ id }
  //       data-testid={ dataTestId }
  //       value={ value }
  //       onChange={ this.handleChange }
  //     />
  //   );
  // }

  render() {
    const { onClick } = this.props;
    const { subtitle, title, imagePath, storyLine, rating, genre } = this.state;
    return (
      <form data-testid="add-movie-form">
        <label htmlFor="title-id" data-testid="title-input-label">
          <input
            type="text"
            id="title-id"
            data-testid="title-input"
            value={ title }
            // onChange={ this.handleChange }
          />
          Título
        </label>
      </form>
    );
  }
}

export default AddMovie;


{ /* <label htmlFor="title-id" data-testid="title-input-label">
          <input
            type="text"
            id="title-id"
            data-testid="title-input"
            value={ title }
            onChange={ this.handleChange }
          />
          Título
        </label>
        <label htmlFor="subtitle-id" data-testid="subtitle-input-label">
          <input
            type="text"
            id="subtitle-id"
            data-testid="subtitle-input"
            value={ subtitle }
            onChange={ this.handleChange }
          />
          Subtítulo
        </label>
        <label hmtlFor="imagepath-id" data-testid="image-input-label">
          <input
            type="text"
            id="imagepath-id"
            data-testid="image-input"
            value={ imagePath }
            onChange={ this.handleChange }
          />
          Imagem
        </label>
        <label htmlFor="storyline-id" data-testid="storyline-input-label">
          <textarea
            id="storyline-id"
            data-testid="storyline-input"
            value={ storyline }
            onChange={ this.handleChange }
          />
          Sinopse
        </label>
        <label htmlFor="rating-id" data-testid="rating-input-label">
          <input
            type="number"
            id="rating-id"
            data-testid="rating-input"
            value={ rating }
          />Avaliação
        </label> */}